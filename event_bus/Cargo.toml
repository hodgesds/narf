[package]
name         = "narf-event-bus"
description  = "Cap-gated SPMC publish/subscribe bus: per-topic ring, per-consumer cursor, gap-signal on slow subscriber. Phase 1 = in-kernel async surface only."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_event_bus"
path = "src/lib.rs"

[dependencies]
narf-lib          = { path = "../lib" }
narf-capabilities = { path = "../capabilities" }
narf-time         = { path = "../time" }
narf-kernel-test  = { path = "../verification/kernel-test" }

[features]
kernel-test = []

[lints]
workspace = true
