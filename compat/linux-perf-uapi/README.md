# Linux perf UAPI

`narf-linux-perf-uapi` contains the `no_std` Rust representation of the Linux
`perf_event_open(2)` wire ABI used by NARF's userspace compatibility layer.
See `specification/spec.md` for scope and provenance.
