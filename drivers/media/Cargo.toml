[package]
name         = "narf-drivers-media"
description  = "Media drivers: DVB, SDR, CEC, and TV Tuners."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_drivers_media"
path = "src/lib.rs"

[dependencies]
narf-lib             = { path = "../../lib", version = "0.1.0" }
narf-bus             = { path = "../../bus", version = "0.1.0" }
narf-init            = { path = "../../init", version = "0.1.0" }
narf-console         = { path = "../../console", version = "0.1.0" }
narf-drivers-usb     = { path = "../usb", version = "0.1.0" }

[features]
kernel-test = []

[lints]
workspace = true
