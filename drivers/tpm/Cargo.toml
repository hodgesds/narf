[package]
name         = "narf-drivers-tpm"
description  = "TPM 2.0 drivers — CRB (fTPM) + TIS (discrete), full TPM2 command set, ACPI probe."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_drivers_tpm"
path = "src/lib.rs"

[dependencies]
narf-lib          = { path = "../../lib" }
narf-kernel-test  = { path = "../../verification/kernel-test" }
narf-filesystem   = { path = "../../filesystem" }
narf-block        = { path = "../../block" }

[features]
kernel-test = []

[lints]
workspace = true
