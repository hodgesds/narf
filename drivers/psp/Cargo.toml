[package]
name         = "narf-drivers-psp"
description  = "AMD Platform Security Processor (PSP) driver — CCP mailbox protocol, TEE channel, firmware-version query."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_drivers_psp"
path = "src/lib.rs"

[dependencies]
narf-lib          = { path = "../../lib" }
narf-kernel-test  = { path = "../../verification/kernel-test" }

[lints]
workspace = true
