[package]
name         = "narf-drivers-leds"
description  = "LED class registry + GPIO/PWM/HID-keyboard LED drivers."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_drivers_leds"
path = "src/lib.rs"

[dependencies]
narf-lib          = { path = "../../lib" }
narf-drivers-gpio = { path = "../gpio" }
narf-pwm          = { path = "../../pwm" }
narf-kernel-test  = { path = "../../verification/kernel-test" }
narf-init         = { path = "../../init" }
narf-scheduler    = { path = "../../scheduler" }
narf-time         = { path = "../../time" }
narf-filesystem   = { path = "../../filesystem" }
async-trait       = "0.1"

[features]
linux-compat = ["narf-filesystem/linux-compat"]
kernel-test = []

[lints]
workspace = true
