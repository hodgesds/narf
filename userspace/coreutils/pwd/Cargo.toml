# Isolated workspace root — must NOT join the kernel workspace.
# See userspace/init/Cargo.toml for the rationale.
[workspace]

[package]
name         = "narf-coreutil-pwd"
description  = "NARF coreutil: pwd — print current working directory."
version      = "0.1.0"
edition      = "2021"
rust-version = "1.85"
license      = "GPL-2.0-or-later"

[dependencies]
narf-libc = { path = "../../../narf-libc" }

[[bin]]
name = "pwd"
path = "src/main.rs"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
missing_debug_implementations = "warn"
