[package]
name         = "narf-efi"
description  = "Transport-neutral UEFI Runtime Services + variable + time codecs. Clean-room from public UEFI 2.10 spec."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_efi"
path = "src/lib.rs"

[dependencies]
narf-kernel-test  = { path = "../verification/kernel-test", version = "0.1.0" }

[features]
kernel-test = []

[lints]
workspace = true
