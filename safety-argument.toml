[safety_argument]
schema_version = 1
agent = "OpenAI Codex GPT-5"
prompt_hash = "a9db3623ff8ec7dd1a0b50343afafd876a0d9007fc4302238549a8f3b3aac75f"

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "NUMA migration is implemented inside the existing memory-manager TCB and does not add a trusted subsystem or move the documented trust boundary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Migration replaces only a process's existing private backing frame. It preserves the original PTE permissions and region ownership, and does not alter capability checks, domain tags, or domain rights."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The old frame is released only after the authoritative backing entry and leaf PTE are replaced and cross-CPU TLB invalidation completes, preventing stale access to reallocated memory and introducing no new isolation exception."
