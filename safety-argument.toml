[safety_argument]
schema_version = 1
agent = "OpenAI Codex GPT-5"
prompt_hash = "6e7ecf10083ec035057bdce834a72bbe2bffae8df9383ed10eb06c78dd70944b"

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The cgroup memory-node constraint is carried by the existing scheduler and memory-manager TCB components and does not add a trusted subsystem or move the documented trust boundary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The allowed-node mask only narrows physical allocation placement. It does not alter mapping permissions, capability checks, domain tags, or domain rights."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Per-task allowed-node state is keyed by scheduler task identity and cleared on detach and exit. Fault-time publication remains per-CPU and is cleared immediately after demand allocation, preventing policy leakage between tasks."
