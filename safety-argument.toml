[safety_argument]
schema_version = 1
agent = "OpenAI Codex GPT-5"
prompt_hash = "ebedd0c0a9b5c6bf58a25e7ede31105d76e0eb74249832cfd8d10a37a0efba29"

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The only frame/ change forwards the existing kernel-test feature to narf-boot so validation smokes enter the distributed test slice. It adds no production code, authority, memory access, domain transition, or trusted subsystem; release builds leave the feature disabled."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame manifest only forwards the existing kernel-test build feature to the untrusted Btrfs driver so its distributed tests and embedded fixtures are linked exclusively into verification kernels. Production frame code, authority, domain transitions, mappings, capabilities, scheduling state, and the TCB boundary are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit only forwards the interrupted instruction pointer already present in its trap frame into the existing IRQ dispatcher; it adds no new TCB member or authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "NUMA procfs reporting copies only virtual metadata and aggregate node counts while the address-space region locks are held; it never exports physical addresses, clones HugeFrame ownership, or changes mapping/domain rights."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Huge mappings validate virtual/physical alignment before installing architecture leaves, retain authoritative frame ownership in AddressSpace, invalidate translations before returning frames to the pool, and preserve W^X plus NUMA/cpuset selection without changing domain assignment."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Explicit NUMA syscalls and cpuset.memory_migrate replace authoritative base-page or complete huge-leaf backing and flush peer TLBs before old frames return to the buddy or huge pool. Failed huge-leaf replacement restores the original mapping and retains its ownership. Shared mappings are never moved without MOVE_ALL authority, which NARF rejects because it has no ambient CAP_SYS_NICE."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Correcting Linux mempolicy flag values and adding PREFERRED_MANY only changes the order in which frames are requested from existing per-node allocators. The cpuset allowed mask remains a hard boundary, and frame ownership, domain assignment, and mapping authority are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Weighted interleave changes only target-node selection before existing owned frame and huge-frame allocation. The policy mask remains intersected with cpuset.mems, weights cannot be zero, and no mapping, ownership, domain-rights, or authority checks are bypassed."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "HMAT automatic weighting publishes bounded allocation ratios through atomics under a configuration lock. Firmware coordinates cannot expand a task's cpuset policy mask or grant mapping/domain authority, and missing coordinates fail rather than fabricating topology."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Moving interleave sequence ownership from CPU slots to task-scoped userspace-compat state changes only deterministic target selection. The active fault policy still intersects cpuset.mems before entering the allocator, and task teardown reclaims the sequence state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Automatic NUMA balancing samples only private, unlocked user leaves and retains ownership of every backing frame while the leaf is absent. Hint resolution intersects policy with cpuset.mems, serializes migration through the address-space region lock, completes cross-CPU TLB invalidation before freeing the source, and always restores a mapping after migration failure."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Memory hotplug admits only aligned, non-overlapping host-acknowledged RAM already covered by the active kernel linear map. Online donation cannot grow allocator metadata under the frame lock; offline is an exact-range, all-free transaction whose failed validation leaves buddy lists and per-node accounting unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "virtio-mem preserves physical-frame ownership by ordering PLUG before allocator donation and allocator removal before UNPLUG, rolling either side back when the second step fails. No busy frame is reclaimed or exposed to the host, and dynamic node reporting exports aggregate state rather than physical addresses."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "HMAT memory tiers only constrain target selection: demotion requires a strictly slower online tier and intersects candidates with the caller-provided cpuset/mempolicy mask. Unknown firmware coordinates remain in the default top tier rather than silently weakening placement policy."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Tier demotion delegates to the existing private-page and complete-huge-leaf migration transaction, preserving ownership and permissions, rolling back failed leaf replacement, and completing cross-CPU TLB invalidation before returning old backing to the allocator."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Shared-memory migration serializes alias creation with replacement, retains both frames through all address-space updates, rolls back every changed alias before failure returns, commits registry ownership only after all PTEs change, broadcasts TLB invalidations, and frees the source only after commit. Device/DMA mappings are rejected."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Memory-block membership is derived from authoritative boot and hotplug physical RAM ranges classified by SRAT proximity, never inferred from CPU placement or fabricated for absent memory."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Externally owned shared pages carry one registry reference per address-space alias. Handle removal prevents new aliases but defers frame reuse until every mapped alias has torn down its leaf and completed cross-CPU TLB invalidation; mapping, unmapping, MAP_FIXED replacement, fork aliasing, and migration use one shared transaction order."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The memory-hotplug observer runs only after allocator donation/removal and topology-registry commit, with both locks released. It receives no physical-frame ownership and can only rebuild read-only sysfs topology; failed and busy transactions emit no notification. Offline ranges remain recorded solely as non-allocatable topology."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "PMI dispatch runs after the trap prologue has entered the Frame domain and only copies scalar task/IP/time values into kernel-owned per-CPU storage; it does not bypass capability checks or alter domain tags."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler PMU hook receives only the already-published scalar TaskId and a run-state edge. It runs outside run-queue locks, grants no capability, and cannot select or resume a task."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "PMU switch-out disables and releases the current CPU's counter before executor code or a peer task runs. Per-CPU allocation prevents one logical CPU from programming or releasing another CPU's MSR bank during migration."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The aarch64 boot edit passes only a firmware-validated private interrupt number from the bus parser to the GIC. The GIC rejects non-PPI INTIDs, applies the route only to each CPU's own redistributor, and grants no capability or cross-domain memory access."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The aarch64 IRQ edit publishes only the interrupted ELR scalar through the existing per-CPU interrupt context slot after the trap prologue has entered the Frame domain. It neither dereferences task memory nor changes domain rights, capabilities, or scheduling state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The NUMA topology fallback is compiled only for hosted crate tests, where the narf-frame ACPI bridge is absent. Kernel targets still require the authoritative frame-provided hooks, so production frame placement, ownership, cpuset constraints, and domain assignment are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Adding NetfilterAdmin as a distinct stable capability kind keeps firewall authority in the existing capability-table TCB and does not expand the trusted subsystem set."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "A netfilter control operation requires a live epoch-checked capability, sufficient explicit operation rights, and an exact immutable network-namespace-id match. No uid, Linux ambient capability bit, namespace membership, or raw userspace slot bypasses those checks."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L70"
argument = "NetfilterAdmin copies share one capability-table object epoch; revoking one invalidates every copied handle at its next authority check while revealing only the already documented stale-cap revocation bit."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The procfs adapters read only the scheduler-published current task id, resolve its immutable network-namespace identity through userspace compatibility state, and request read-only aggregate snapshots. They neither dereference userspace memory nor modify capability, domain, frame, or scheduling state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Network namespace lifetime is retained by typed Arc ownership in tasks, namespace fds, and sockets. Final-reference teardown removes only state keyed by that immutable namespace id, returns physical interfaces to namespace zero after releasing registry locks, and does not alter memory domains, frame ownership, capability epochs, or scheduler state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Perf code-page-size sampling reads only registered virtual-region bounds and the hardware leaf-size enum under existing address-space locks. It exports no physical address or frame handle, transfers no ownership, and does not modify page tables, domain rights, capabilities, or scheduler state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Perf privilege filtering changes only the current CPU's exclusively allocated PMU selector bits. Existing ownership checks still guard allocation, rearm, pause, read, and release; the filters grant no memory access, capability, domain right, or cross-CPU register authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Perf IRQ capture copies scalar registers from the already-saved architecture trap frame after the trap prologue has entered the Frame domain. The dispatcher publishes them only in kernel-owned per-CPU atomics for the synchronous handler walk; no trap-frame pointer, capability, domain right, or scheduling authority escapes."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "User-stack sampling resolves bytes only through the sampled AddressSpace's locked, authoritative resident backing metadata and the kernel direct map. It stops at unmapped or lazy backing, never faults user pages in IRQ context, never exposes physical addresses, and retains all frame ownership."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit changes only the diagnostic time budget attached to the existing verification-only Btrfs interop initcall. It adds no trusted subsystem, execution path, authority, capability, mapping, domain transition, or ownership change; the initcall body and result handling are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler and frame edits remove per-slot pop/requeue and per-executor-round diagnostic atomic updates from the scheduler hot path while retaining bounded, cold-path queue, affinity, halted, and lock-state fatal diagnostics inside the existing TCB. They add no trusted subsystem and do not move the documented TCB boundary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Removing observational hot-path counters does not change the wake flag, ready queues, task affinity, context/domain restore, capability checks, domain tags, PKRS state, or domain transitions. The retained watchdog snapshots are read-only and execute only after a suspected stall."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L62"
argument = "The retained scheduler observations expose only bounded queue depth, awake/home/affinity state, and CPU halted/lock state to the in-kernel fatal watchdog. They remain within the existing documented per-CPU scheduling-state exception and expose neither task memory nor capabilities to userspace."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The direct PID 1 loader remains inside the existing frame TCB and only publishes a task after the existing ELF loader has built its address space and the task's explicit filesystem-root metadata is installed. It grants no capability, changes no domain assignment, and removes a userspace chroot launcher rather than adding an authority-bearing intermediary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The rooted interpreter lookup reads bytes only through the existing VFS resolver under the explicit mounted root and retains the normal ELF-loader validation path. It does not dereference untrusted user pointers, alter page mappings outside the loader, or bypass capability or domain checks."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The cgroup memory-charge resolver maps an executor-private TaskId to the already authoritative userspace ProcessId before consulting existing cgroup membership. It adds no capability, domain transition, memory mapping, or frame-ownership path; unmapped kernel tasks remain deliberately unattributed rather than being charged to an accidental numeric TaskId match."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit only links the untrusted SquashFS driver crate and invokes its initcall-registration function alongside existing filesystem drivers. Parsing and block I/O remain in DomainId::DRIVER_0 behind the existing filesystem/block interfaces; no capability, domain transition, frame ownership, scheduler state, or TCB boundary changes."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit embeds a verification-only static test binary in the existing boot-init fixture. It adds no production execution path, authority, mapping, capability, domain transition, or ownership change."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The mount-change bridge invokes the existing global readiness notifier only after filesystem state has committed and released its mount-table lock. It carries no path, task, capability, mapping, domain, or ownership authority; waiters independently re-query their own namespace-scoped procfs view."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Two-CPU user-task placement changes only the preferred initial run queue within the existing CpuSet::ALL allowed mask. It does not expand the TCB, alter a task's domain, capability, address space, or saved security state, and larger topologies retain the existing AP-only policy."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The placement helper selects only already-online scheduler CPUs and enqueue_on continues to enforce the existing task metadata and domain restore path before polling. Alternating BSP/AP grants no memory or capability authority and never schedules outside Affinity.allowed."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Runtime affinity changes remain inside the existing scheduler TCB and change only a live task's allowed online CPU set. The task retains its TaskId, address space, domain, capabilities, saved protection-key state, and dedicated kernel stack across the cooperative requeue."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "A parked task is removed under its sole run-queue lock and enqueued only after that lock is released; a currently-executing task is never moved mid-continuation and consumes the registered mask only after returning to the executor. Every dispatch and work-steal path checks the hard allowed mask before restoring task state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Task creation validates caller-provided affinity against the online topology before publishing either the run-queue slot or registry entry. Because the legacy spawn API is infallible, a wholly offline mask is normalized to the spawning CPU while a partly online mask is retained for hot-plug; this prevents an awake slot from cycling forever without weakening any valid hard affinity constraint."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Post-poll requeue keeps the CPU that just executed the continuation whenever it remains online and allowed. Affinity.preferred is only an initial-placement hint; it cannot force an otherwise-unnecessary cross-core context migration or bypass the hard allowed mask."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The BPF memory work stays inside the existing frame/ + memory/ TCB and adds no new trusted subsystem. `bpf_text::reserve_kernel_slots` runs as a direct call from bare_main after the MMU handoff and before the first `new_user_pml4`, because `new_user_pml4_on` snapshot-copies PML4[256..511] by value with no propagation; a slot first populated after a user address space exists would leave that CR3 holding a zero entry and triple-fault. A `debug_assert` in `new_user_pml4_on` makes a future reordering fail loudly, and an in-kernel smoke reads the live root to assert both entries are present and both arena guard slots are absent."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Every BPF kernel-VA mapping is installed with USER clear at the leaf and at every intermediate level, so no CPL=3 / EL0 walk can reach BPF text, arena pages, or the per-CPU BPF stack. Arena frames are zeroed on population so a recycled frame cannot leak its previous owner's bytes to a program. The extable consults a per-image table keyed on the faulting PC and recovers only kernel-mode faults; it neither bypasses nor alters any capability check or domain tag."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "`CapKind::Jit` gates both executable-text allocation and the only mprotect path that can produce a W|X user mapping. Both entry points re-check liveness on every call rather than trusting possession, per invariant #5; the grant table is swept by the thread-scoped exit observer so an authority cannot outlive its task; and adding W to an already-executable mapping is refused regardless of the capability. `AddressSpace::mprotect_range` continues to reject W|X outright, so exactly one capability-gated function can construct such a mapping."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Kernel mappings are NX by default and the remaining W^X gap is recorded rather than claimed closed. `memory/src/x86_64/mmu.rs` now builds the low identity map, the high-MMIO window, the kernel direct map and the higher-half kernel window with NO_EXEC, demoting the identity map 1 GiB -> 2 MiB -> 4 KiB and the kernel window 1 GiB -> 2 MiB so the only executable ranges are [__kernel_start, __text_end) and the 8 KiB AP-trampoline window at physical 0x8000; `frame/src/aarch64/boot.S` does the same with PXN|UXN over both 1 GiB RAM blocks. Both demotions are constructed before the CR3 / sctlr_el1 handoff, so no live mapping is ever split and no break-before-make window exists. The AP trampoline sets EFER.NXE before CR0.PG, because with NXE clear PTE bit 63 is a reserved bit and the first post-paging fetch would triple-fault. The x86_64 S3 wake trampoline in `arch/src/x86_64/s3_resume.rs` has the same dependency and now discharges it the same way: firmware is permitted to clear IA32_EFER across S3, so `save_resume_context` snapshots EFER (with LMA masked off, as Linux does for `tr_efer`) and `s3_wake_entry` replays it with `wrmsr` BEFORE the `mov cr3` that switches to the NX kernel tables — otherwise the first access through any kernel mapping, data or fetch, would take a reserved-bit #PF with no IDT loaded. This is reasoned-and-unit-tested, not boot-proven: S3 remains gated off behind `REAL_SLEEP_ARMED` / `PRODUCTION_S3_ENABLED` and QEMU/TCG gives no real suspend/resume cycle, so the smokes in `arch/src/tests.rs` (`arch/s3`) pin the saved EFER's shape (LMA masked, LME and NXE set) and the `offset_of!`-derived displacements the asm indexes, rather than observing a resume. Consequently no kernel data has an executable alias, and BPF JIT text is executable at exactly one address. It is no longer WRITABLE through that alias either: `memory/src/text_poke.rs` `protect_ro`, called from `bpf_text::seal`, clears WRITABLE / sets AP_RO_EL1 on every kernel window aliasing the pack's frames — the low identity map, the higher-half kernel window, and the direct map where one exists — so an arbitrary kernel write can no longer overwrite live JIT text. On x86_64 a live huge leaf in the way is split following Linux `__split_large_page` (`arch/x86/mm/pat/set_memory.c:1121`): the replacement table translates identically to the leaf it replaces, so Intel's TLB application note (undefined only when the large and small translations DIFFER) does not apply, the swap is one naturally-aligned 8-byte store, and the attribute change happens only after a synchronous global flush (`flush_user_tlb_all_cpus`, which spins for every peer's ack), which is Linux's split -> flush_tlb_all -> change ordering; `bpf_text`'s PACKS lock serialises it as `cpa_lock` does. This also required setting CR0.WP, which NARF's boot path never did (measured CR0 was 0x80000011) and without which a supervisor store ignores the R/W bit entirely (Intel SDM Vol 3 §4.6.1) — `text_poke::enable_write_protect` sets it on the BSP and on every AP, since WP is per-CPU state. Writes into an already-sealed pack go through `text_poke::poke_copy`, a per-CPU RW+NX alias of one 4 KiB frame torn down before the call returns, and `reclaim` calls `protect_rw` before frames go back to the buddy. One case is deliberately NOT closed: an aarch64 fallback pack (scattered 4 KiB frames, built only when the hugepage pool is empty) would need a live 2 MiB block split, which ARMv8 requires break-before-make for and which would unmap live kernel memory on every other CPU — Linux arm64 declines the same split for the same reason. `can_protect` refuses it, `seal` leaves the pack's alias writable, and `bpf/specification/spec.md` §4.2 / §8.6 record the trade. In-kernel smokes in `memory/src/tests.rs` prove the alias faults on instruction fetch through both windows and that the trampoline window is exactly two executable pages wide; `memory/src/bpf_text.rs` `smoke_bpf_text_sealed_alias_write_faults` performs the write that worked before this change through the linear alias and requires a #PF with the present+write error bits set and the byte unchanged, and `smoke_bpf_text_reclaim_restores_writable_alias` proves the frames go back writable."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "`validate_user_range` — the single choke point every `copy_to_user` / `copy_from_user` / `copy_from_user_vec` / `copy_user_cstr` call site in `userspace/` funnels through — now confines BOTH the first and the last byte of a syscall's range to the user half (`addr < 1 << 47`, Linux's `__access_ok` against `TASK_SIZE_MAX`). It previously accepted canonical kernel-half addresses on the recorded theory that SMAP enforced the boundary. That argument was inverted: SMAP faults a CPL-0 access to a page with PTE.U=1, says nothing about a kernel page (U=0), and `copy_user_guarded` runs its `rep movsb` inside a STAC/CLAC bracket that disables SMAP outright — so a mapped kernel destination was written silently, the guarded copy's fault path never fired, and the caller got Ok(()). Any syscall with a caller-supplied (address, length, content) triple was therefore an arbitrary-kernel-write primitive; `bpf(BPF_BTF_LOAD)` followed by `bpf(BPF_OBJ_GET_INFO_BY_FD)` with `bpf_btf_info.btf` in the kernel half was the demonstrated gadget, and the read direction was the matching info leak. The check is unconditional in every production build: the only relaxation is `handlers::kernel_buffers_guard()`, which is `#[cfg(feature = \"kernel-test\")]`, dynamically scoped to a block, keyed on the calling CPU, and still refuses a range that straddles the canonical hole. It is held by the `syscall_abi` fixture harness and 125 named in-kernel smokes (126 guard sites, one of which is `with_kernel_buffers` itself) that hand kernel `.rodata`/stack/heap pointers to the syscall entry point; every other test in the `kernel-test` build, including the seven in `userspace/src/abi_uaccess_tests.rs` and `smoke_uaccess_validate_canonical_holes`, exercises the production predicate."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Removing BuddyZone::free's vestigial overlap scan changes only the complexity of the existing allocator free path. Frame accounting and standard buddy coalescing remain unchanged inside the memory TCB, and no new trusted subsystem or ownership path is introduced."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The x86_64 trap-entry edit remains inside the existing frame TCB and only normalizes the live direction flag before compiler-generated kernel code executes. It adds no trusted subsystem, authority, domain transition, mapping, or ownership path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The CPU has already saved the interrupted RFLAGS in the trap frame before CLD executes, and IRETQ restores that saved value unchanged. Clearing only the live kernel DF prevents attacker-controlled CPL3 flag state from reversing kernel string operations without changing capability checks, domain tags, saved user state, or return authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler edit adds accounting callbacks at existing x86_64 stackful switch boundaries inside the already enumerated executor TCB. It does not add a trusted subsystem, change task selection, alter a task's saved domain state, or move any capability or address-space ownership."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Trap-frame and kernel-continuation layouts move to the existing architecture TCB boundary and are consumed by frame and the executor without duplicate structs or pointer casts. The change adds no trusted subsystem or policy authority; scheduling policy crates still receive neither trap frames, hardware rights, stacks, nor the switch entry point."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Every common x86_64 trap, fast syscall, and aarch64 vector captures live PKRS/CR3 or SCTLR/GCR state and enters neutral Frame execution before any Rust runs. The architecture switch primitive masks interrupts, neutralises rights before its first outgoing-context store, reads the complete incoming context while neutral, and restores rights only after its final incoming-context load. Trap-preempted tasks resume the neutral handler continuation before the assembly epilogue restores the interrupted rights."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L67"
argument = "Hardware domain state remains opaque task context. Cooperative stackless tasks retain memory::DomainSavedState; stackful tasks retain only architecture-private raw fields, and the new kernel smoke proves that a task yielding under BPF confinement neither leaks its rights to the executor nor loses them on resume. No scheduler policy callback observes the snapshot."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "On PKS systems each dedicated atomic-BPF stack leaf is tagged DomainId::BPF and is used only while FRAME+BPF rights are active. The confinement guard is !Send and holds the scheduler's nestable preemption guard so a CPU-local rights snapshot cannot migrate before restore. Sleepable BPF constructs and drops both guards around each individual poll, returning Pending only after neutral scheduler rights have been restored; its heap stack remains explicitly documented as FRAME-owned pending a domain-aware allocator."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Timer-preemption accounting reads only the current task's scalar timestamps while its continuation is pinned on the current CPU. The pause callback completes before CURRENT_STACKFUL_TASK is cleared, the resume callback runs only after the same continuation is current again, and both use IRQ-safe per-task accounting locks. Normal syscall entry creates the task's BTreeMap row before publishing the active span, so the timer-IRQ close path only updates an existing node and cannot allocate; no user pointer, capability, domain right, or cross-task memory is exposed or modified."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The removed scan neither enforced capability checks nor assigned domain tags; it leaked an overlapping free after an ownership violation had already occurred. The audited address-space and page-table ownership fixes remain in place, while KASAN-capable kernel tests and stress teardown continue to exercise double-free and use-after-free failures."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The boot huge-page reservation and per-NUMA-node pool remain inside the existing memory/frame TCB. The change adds no trusted subsystem, capability, domain transition, or user-visible authority; it makes the boot-only physical-memory ownership contract explicit with an unsafe interface."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Huge-page reservation now excludes the architecture-reserved low-memory window and every caller-protected physical range before publishing a HugeFrame, with bare_main mandatorily protecting the complete loaded-kernel interval before the buddy sees the same usable map. Each claimed frame is stored in exactly one physical-NUMA-node stack and returned as a buddy exclusion; boot reserves stack capacity for allocated plus free claims, so free_hugepage cannot recursively allocate while holding the IRQ-safe pool lock."


[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit only force-links the optional narf-bpf-leds seam and registers its initcalls beside the existing BPF consumer anchor. The new kfunc, bounded mailbox, LED registries, and worker remain outside the documented TCB; no trusted subsystem, capability authority, domain assignment, task state, or memory-mapping path is added."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "narf_led_submit accepts scalars only and, in atomic BPF context, performs a bounded sequence of atomic operations into fixed FRAME-visible storage. Device lookup, allocation, trigger locking, and potentially sleepable hardware access occur later in one Stage::Late worker. Unknown actions fail with EINVAL, queue saturation fails with EAGAIN, full-width invalid indices remain drain-time no-ops, and the kfunc neither dereferences program pointers nor changes capability or domain rights."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Dynamic packet bounds change only the existing untrusted BPF verifier/runtime and Linux-compat attach path; no frame, memory-domain-manager, capability-table, scheduler-executor, or security-model code is modified, and no subsystem is added to the documented TCB."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "An XDP packet address is constructed only from the classifier's live immutable frame borrow after a BpfAttach-authorized type-correct attach. The verifier requires a same-provenance unsigned data/data_end comparison covering the complete access; raw-context execution is refused; the interpreter independently checks the exact slice; and native code is admitted only at the verifier-certified site. Execution remains inside the existing FRAME+BPF PKS mask, so the feature exposes RX bytes reachable there and grants no access to driver-private state or any additional domain."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "XDP test-run changes only the existing BPF runtime and Linux-compat syscall adapter. It modifies no frame, memory-domain-manager, capability-table, scheduler-executor, or security-model code and adds no trusted subsystem."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The syscall range-validates and copies data_in into a size-bounded kernel-owned allocation before execution, then BpfProg::run_xdp alone constructs data/data_end from that exact immutable slice. Caller-supplied ctx pointers, CPU selection, flags, and batch mode are rejected; raw XDP execution remains unavailable; repeat is bounded; output copies only the owned frame prefix through the guarded user-copy path; and verifier, interpreter-region, and JIT admission checks remain unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Native arena-atomic lowering changes only the existing untrusted BPF JIT and runtime plus their specification and tests. It modifies no frame, memory-domain-manager, capability-table, scheduler-executor, or security-model code and adds no trusted subsystem."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The verifier remains the sole admission path for native arena atomics. Both emitters derive the address from the page-aligned kernel arena slot base plus the verifier-tracked zero-extended handle and signed instruction displacement, reject misalignment before any atomic access, and register the actual faulting instruction for recovery before executable publication. Natural-alignment and page-fault paths return distinct terminal statuses with the offending handle; fetching bitwise arena atomics preserve the portable fail-closed boundary and interpret instead of using x86 scratch state that would destroy that handle."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The production fix only moves the existing TSS.rsp0 and per-CPU SYSCALL-stack hook installation from the x2APIC-only IPI branch to the common x86_64 LAPIC initialization path. Frame and the scheduler executor remain within the existing TCB set; no subsystem, authority, or TCB boundary is added."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The retarget hook continues to write only the current CPU's existing TSS.rsp0 and PerCpu.kernel_stack_top from a scheduler-owned task stack bound. Installing it for xAPIC restores the same per-task isolation already used by x2APIC and prevents user syscalls from sharing and corrupting the executor stack; capability checks, domain tags, address spaces, and task ownership are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Private base-page teardown skips the external shared-alias transaction only while holding the address-space region tables continuously from SHARED classification through removal. Shared unmap and range punching retain transaction -> huge-region -> regular-region ordering through leaf removal, cross-CPU TLB invalidation, and the backing owner's release hook. A racing map cannot change the classification without the held region lock. The x86_64 range helper holds the same per-root page-table mutation lock across the batch and caches only non-owning leaf-table pointers while that lock excludes replacement. Small batches retain per-leaf INVLPG; large batches clear every leaf before one compiler-fenced local non-global flush. Callers retain the existing cross-CPU flush-before-frame-release ordering. Private COW ownership is unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Range-scoped mmap materialization holds the authoritative address-space region lock continuously while selecting current backing metadata and installing leaf PTEs, so a CLONE_VM peer cannot unmap and free a cloned snapshot beneath the walk. It validates a non-empty aligned user-half interval and reuses the existing per-architecture permission derivation, W^X policy, map-error handling, and frame ownership; it changes only which current region page slots are visited and grants no new capability or domain access."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Fork COW state is represented independently from logical POSIX WRITE authority. Page-table derivation permits a writable user leaf only when WRITE remains granted and either no COW sharing exists or the authoritative frame refcount is one. The write-fault path requires both WRITE and COW before copying, so mprotect(PROT_READ) cannot restore access. aarch64 user mappings use EL0 AP encodings, and lazy zero sentinels never become leaves."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Private-region relocation holds huge and regular region locks from validation through publication, preinstalls a disjoint destination before removing source leaves, and retains source bookkeeping for rollback on page-table failure. A destination accepted by both region tables is PTE-free under the mapping invariant because every metadata removal first tears down its leaves; an unexpected leaf is rejected by installation and rollback rather than overwritten. Scatter installation holds the existing per-root page-table mutation lock across the batch, derives each page's unchanged COW/user permissions, and skips lazy zero sentinels. Source translations are invalidated on every CPU before a truncated tail is returned to the allocator; shared and huge ownership shapes are rejected rather than approximated."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "mlock validates complete mapped coverage before mutation, resolves file-demand pages through their external owner, allocates anonymous backing with no region lock held, revalidates before publishing, and frees every unconsumed or failed allocation. Exact VMA splitting moves rather than clones physical lists, preserving private, COW, and borrowed-frame ownership; LOCKED changes reclaim eligibility only and grants no mapping permission."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "mprotect and madvise preflight complete range coverage before changing permissions or releasing backing, so a hole cannot cause a partial security-state change. Permission rewrites preserve internal LOCKED/COW flags, derive hardware access from the resulting logical authority, skip lazy zero sentinels on both architectures, and keep W^X rejection in the existing capability-gated transition model."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Residency queries hold the huge-region and regular-region locks in the established order for one coherent snapshot, validate complete coverage before returning any result, and expose only one resident/nonresident bit per requested user page. They neither materialize lazy backing nor reveal physical addresses, frame ownership, COW reference counts, capability state, or domain tags; the userspace mincore adapter copies only this bounded bit vector through the guarded user-copy path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The base-page region table remains ordered by virtual base after every operation that can move or append an entry. Since overlap admission still rejects intersecting intervals, predecessor/successor checks are equivalent to the former full scan. Empty MAP_FIXED punches return before touching page tables or backing; overlapping punches retain the existing under-lock leaf removal, cross-CPU invalidation-before-release, and authoritative Region.phys ownership path. Keyed removal does not change which region or frames are retired."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Exact-range LOCKED flag updates use the ordered, non-overlapping VMA invariant to drain only intersecting tree entries. Each affected Region.phys vector is still consumed exactly once into disjoint head/middle/tail fragments, unchanged regions remain in place, and reinsertion derives each key from its fragment base. This changes metadata movement only: eager population, leaf permissions, COW/shared ownership, and failure-before-mutation coverage validation are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Overlapping MAP_FIXED and munmap punches use the ordered, non-overlapping VMA invariant to classify shared mappings and drain only intersecting tree entries. Each removed Region.phys entry still follows the existing under-lock leaf teardown and shared-release or private-free path; prefix and suffix fragments remain disjoint and are reinserted by base. Unchanged regions retain ownership in place, and cross-CPU invalidation still precedes private frame reuse."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The base-page VMA representation changes from a sorted vector to a BTreeMap keyed by Region.base. Structural mutation is private to RegionTable: insert derives the key from the value, relocation and stack growth remove the old key before changing base, and range splits drain owned Region values before inserting disjoint fragments. Tree removal therefore transfers each Region.phys vector exactly once into the same leaf-teardown, COW/shared release, or surviving-fragment paths; it changes lookup and metadata movement complexity without changing frame ownership, page-table mutation, or invalidate-before-reuse ordering."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Ordered-tree point lookup returns only the greatest VMA base not above the requested user address and then checks the region end before exposing or mutating backing. Permission derivation, user-half validation, W^X checks, COW authorization, and domain/page-table flags are unchanged; the index neither grants a mapping nor bypasses any capability or domain check."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The page-table ownership registry remains fixed-size, allocation-free, and lock-free. Open-addressed insertion uses compare-exchange and never replaces a live physical-frame marker; lookup follows a collision chain until a never-used slot; unregister publishes a tombstone rather than an empty slot so later colliding registrations cannot become false negatives. A full registry still fails closed by retaining every existing ownership marker and incrementing the overflow counter. Consequently free_user_pml4_tree continues to reclaim only registered AS-private tables and cannot mistake copied kernel-shared page tables for owned frames."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Live batched swap-out accepts only resident, anonymous-private, base-page backing that is neither shared, COW, file-backed, nor locked. The AddressSpace marks every victim Evicting while holding its authoritative region lock; the swap transaction then revalidates one root, every present leaf, and every physical address before replacing any PTE. Only after the complete PTE batch commits does the ownership callback clear the corresponding Region.phys entries, and only after the targeted TLB retirement completes are the frames returned to the allocator. Swap-in reads into fresh unpublished frames, atomically validates and replaces the complete same-root leaf batch, publishes those frames into Region ownership, and discards the slots only after publication. Failure rolls back transition metadata or retains stable swap entries, so neither path creates an unowned live frame, double owner, or stale translation to a reused frame."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The proportional-set-size range planner is a pure, bounded metadata policy and cannot unmap, free, or grant access. PSS units rank and size fair scan work, while only independently supplied expected_free_pages advances the physical high-watermark goal; a shared alias with proportional charge but zero physical yield therefore cannot satisfy reclaim. The AddressSpace executor revalidates root identity, VMA kind, LOCKED/COW/shared/file exclusions, residency, and leaf ownership before each bounded swap transaction, so stale planner input fails without bypassing mapping permissions, ownership, capability checks, or domain tags."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Targeted x86_64 process shootdown publishes PCID-0 residency before MOV CR3 and clears it only after the plain kernel-root CR3 restore has locally invalidated that context. Filtering becomes authoritative independently per tag bucket, so activity in another bucket cannot turn an untracked tag into a false-negative empty mask. A CPU publishes idle before either scheduler halt; a sender records full-flush debt and then rechecks idle, while wake clears idle before claiming debt, proving that a race is covered by either the acknowledged IPI or a complete local non-global flush before the next task context load. Address-space range/full paths use the remote-only filtered surface only after their local page-table invalidation, and frame ownership is still released afterward. Kernel-global paging hooks remain unfiltered. Tagged aarch64 invalidation uses architected Inner Shareable TLBI propagation and therefore omits only a redundant SGI; untagged VMALLE1 requests retain software fan-out."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Batched reclaim, swap ownership transitions, and targeted invalidation remain within the existing memory domain manager and interrupt delivery seams. They add no trusted subsystem, capability authority, DomainId, domain-rights transition, or cross-domain data path; the change restructures existing page ownership and translation retirement inside the documented TCB boundary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Batched huge-page allocation computes policy/cpuset-constrained node orders before taking the pool lock, transfers ownership only after every requested frame has been popped, and on exhaustion pushes each partial frame back into its exact originating node stack before returning. sys_mmap zeroes the exclusively owned completed vector before publishing any hardware leaf; map_huge_region retains its existing rollback and TLB-before-free ownership ordering."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "x86_64 multi-leaf huge mappings hold the existing root-sharded page-table mutation lock once across the complete fresh-leaf installation. The lock-free helpers remain crate-private and require that guard; public single-leaf callers retain identical validation and locking. A failed batch drops the guard before removing installed leaves, returns every exclusively owned HugeFrame to its exact NUMA pool, and publishes no HugeRegion metadata, preserving one-owner and invalidate-before-reuse ordering without a recursive-lock deadlock."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "aarch64 AddressSpace activation installs the live L0 root owned by that address space together with a lifetime-exclusive process ASID; the executing kernel, stack, and physical-memory accessors remain on the shared TTBR1 high half. The scheduler saves the complete incoming TTBR0, installs the task context before polling, and restores the saved context before any subsequent task without discarding unrelated tagged translations. Domain tags 1..=16 and process tags 17..=the architectural maximum are disjoint. Last-owner teardown broadcasts ASIDE1IS and waits for its barriers before clearing allocator ownership, while exhaustion uses ASID 0 plus a local full invalidation on each distinct-root switch. Page-table mutation uses all-ASID inner-shareable VA invalidation, so editing an inactive process root or shared TTBR1 leaf cannot leave a stale nonzero-tag translation on any CPU."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The aarch64 trap-back longjmp stays on the shared TTBR1 kernel stack and returns from UserTaskFuture::poll with that task's still-live tagged TTBR0 installed. The scheduler slot retains its AddressSpace Arc while the already-tested poll boundary restores the complete incoming TTBR0, so the ASID/root cannot retire during the handoff. Removing the redundant same-context TTBR0 write and VMALLE1 therefore changes no accessible mapping window while preserving tagged translations across traps."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Buddy free-list mutation remains protected by an IRQ-safe lock, but ownership is now partitioned one lock per physical NUMA node. Allocation holds only the selected node lock and releases a failed node before nearest-node fallback; free derives the same authoritative physical node before taking its lock. Boot rebalance takes zone locks in ascending node order and publishes NUMA-aware routing only after every block and stable node total is installed. Runtime online/offline keeps range admission and the selected zone mutation in one transaction, updating atomic totals only after successful donation or complete-range removal. Thus no frame can enter two free lists, become visible before accounting, or be reused while still allocated; cache-line isolation changes contention, not frame authority or domain access."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The bounded order-0 frame cache is partitioned by CPU and physical NUMA node and protected by a cache-local IRQ-safe lock. A buddy refill records each retained cache frame free in the existing allocation-audit bitmap; cache pop records ownership before publication; cache free records relinquishment before insertion; spill/coordinated drain coalesces an already-free frame without a duplicate audit transition. Cache counters remain part of allocator free-page and order-0 reporting, while cgroup charge/uncharge and COW last-owner checks stay outside and precede the cache transition exactly as they precede direct buddy operations. A per-node bypass is published and rechecked under the cache lock before coordinated draining, preventing a racing insertion from escaping a high-order retry. Runtime-hotplug nodes bypass caching before donation and until their last range is offline, so exact free-range removal retains its all-or-nothing visibility and no removed frame remains cached."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Page-scoped demand-fault tickets remain private address-space metadata inside the existing memory TCB. They add no trusted subsystem, capability, domain assignment, user-visible identifier, or cross-domain data path; the change narrows the duration of an existing VMA critical section."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "A demand ticket grants one fault path temporary ownership of an otherwise-unbacked virtual page, not of a published frame. Anonymous allocation and zeroing run outside the region lock, but publication of Region.phys and leaf installation are one region-lock transaction. Removing or splitting a VMA cancels every covered ticket before replacement metadata can appear; a losing anonymous path frees its unpublished frame and a losing file-backed path releases its external alias. Thus no cancelled fault can publish into an ABA replacement, no peer observes half-published backing, and frame reuse still follows leaf retirement and authoritative region ownership."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "A COW ticket pins the exact source frame before releasing the authoritative region lock, so concurrent VMA teardown can drop its ownership but cannot recycle the source during the copy. Publication revalidates the ticket, WRITE+COW authority, virtual-page identity, and unchanged source backing under the region lock. Success transfers Region.phys to the unpublished destination before dropping both the old ownership and temporary pin; cancellation frees the destination and drops only the pin. The existing post-split remap path still derives user permissions from current metadata and performs translation invalidation before either frame can be reused."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Fork's batched COW retain runs inside the same authoritative parent-region transaction as COW marking and before the child backing snapshot is published. It excludes SHARED mappings and zero sentinels, partitions only by the existing physical-frame shard function, and performs exactly one increment for every resident private input occurrence while holding that frame's existing shard lock. Grouping changes lock acquisition count but not owner cardinality, frame identity, or the requirement that every child reference exists before parent teardown can recycle backing."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The ordered NUMA candidate seek is read-only metadata selection under the existing address-space region lock. It starts at the containing or successor VMA, rejects SHARED, LOCKED, inaccessible, and unbacked pages exactly as before, and returns no permission or ownership token. protect_numa_hint_page still revalidates the selected address under the region lock before removing a leaf, while the fault path retains its existing policy, cpuset, migration, and remap checks; shortening the search cannot grant access or recycle backing."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Batched COW count snapshots preserve one result per input position and read every frame under the same physical-frame shard lock as scalar count and retain operations. Materialization and permission rewriting hold the authoritative address-space region lock across snapshot and PTE installation. The x86 scatter installer passes the original backing index to permission derivation even when a zero lazy slot is skipped, so a count cannot be applied to the wrong physical leaf. A count may fall after observation, which only leaves a sole-owner leaf conservatively read-only; the transition from sole owner to shared requires clone_for_fork to acquire that same address-space lock before incrementing, so no snapshot can observe one and then install WRITE over newly shared backing. Permission and TLB derivation otherwise remain unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Batched region, MAP_FIXED-punch, and MADV_DONTNEED release begins only after every affected leaf is removed and the existing local/remote TLB retirement completes. The buddy allocator filters forbidden low memory before refcount mutation, drops exactly one owner for every nonzero input under that frame's existing COW shard lock, and forwards only unregistered or final-owner frames to the same scalar cgroup-uncharge, scrub, allocation-audit, NUMA-cache, and buddy-free path. Duplicate inputs retain scalar owner cardinality, zero sentinels are ignored, and pluggable allocators inherit a scalar batch default, so batching cannot create a second free-list owner or make backing reusable while a COW owner or stale translation remains."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Final-owner batch publication reserves its result capacity before any COW decrement can reach zero and thereafter uses only fixed 64-frame stack buffers, so teardown cannot recurse into frame allocation after acquiring exclusive ownership. Every frame completes cgroup uncharge and optional scrub before publication. Cache insertion and audit transitions match scalar return; any displaced cached entries reach the buddy while the same cache lock remains held, preserving the cache-drain and runtime-hot-remove invariant that a free frame is continuously visible in either the cache or its NUMA zone. Direct bypass publication serializes on the existing zone lock, and the bounded chunk limits IRQ-masked work without changing owner cardinality."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Address-space byte accounting now walks immutable region lengths under the existing region lock and uses saturating addition, rather than cloning ownership-bearing per-page backing vectors. It neither changes mappings nor exposes physical addresses, cannot wrap into a smaller accounting value, and performs no allocation while an exiting task snapshots rusage, so memory pressure cannot convert diagnostic accounting into a kernel allocator panic."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "aarch64 map, unmap, huge-leaf, and final user-root teardown paths now serialize structural page-table writes through a cache-line-independent 64-way lock selected solely from the live root physical page. Scatter installation retains that lock across every intermediate-table and leaf write and executes one architecture-required descriptor-publication barrier before returning, including after partial failure. Contiguous teardown clears every present leaf under the same lock, then executes all-ASID inner-shareable last-level TLBI operands inside one complete DSB/ISB sequence before authoritative backing can be released. MAP_FIXED, MADV_DONTNEED, and region teardown retain their region-metadata transaction through this operation, so no racing writer can orphan a child table, preserve a stale translation to reusable backing, or publish metadata inconsistent with the leaf tree."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The aarch64 root-sharded locks and batched descriptor/TLBI helpers remain entirely inside the existing memory domain manager TCB. They add no capability, domain identifier, cross-domain path, or accessible mapping; they serialize and batch the architecture operations already required for the same page-table authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The aarch64 scatter permission rewriter holds the live root's existing mutation shard across the complete architecture break-before-make transaction. It clears every old leaf before issuing one all-ASID inner-shareable invalidation whose trailing DSB/ISB completes before any replacement descriptor is stored, then publishes all non-zero replacement leaves with one DSB/ISB sequence. Address-space permission changes retain the authoritative region lock and backing ownership through that transaction; zero lazy sentinels remain unmapped. Parent rematerialization now applies the same rewrite after fork, so both parent and child lose write permission before shared COW backing can be modified without a fault."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Batched aarch64 permission rewriting and parent COW write protection stay within the existing memory domain manager and reuse its root lock, region ownership, and translation-invalidation authority. They add no capability, domain identifier, trusted subsystem, cross-domain data path, or new accessible mapping."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The x86_64 scatter permission rewriter retains one existing root-sharded mutation lock across every clear-and-reinstall pair, while the authoritative region lock keeps each non-zero backing frame live and prevents concurrent unmap or replacement. Before releasing the root transaction it locally invalidates the complete changed span, using bounded per-page INVLPG or one non-global flush. A peer-active address space then broadcasts only the remote range half, or uses the existing full local/remote flush for a large rewrite; no peer can retain permissions older than the metadata published by mprotect or COW rematerialization. Zero lazy slots remain absent and permission-only rewriting never releases backing, so batching changes neither ownership nor accessible physical addresses."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The x86_64 one-lock permission transaction and remote-only range broadcast remain private implementation paths inside the existing memory TCB and its established interrupt shootdown bridge. They add no capability, domain assignment, trusted subsystem, user-controlled authority, or cross-domain data path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The aarch64 contiguous clear walker caches only the L3 pointer reached through verified valid table descriptors while holding the same live root's mutation shard. The cache key includes the complete L0/L1/L2 index tuple and is discarded at every 2 MiB boundary, so no leaf write can cross into an unrelated table. Missing tables remain benign holes and a block descriptor fails before it can be treated as a child pointer. Every cleared leaf is still followed by the existing all-ASID inner-shareable invalidation before backing reuse or replacement publication, including partial progress before a structural error; reducing upper-level walks changes neither mapping authority nor translation-retirement ordering."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The cached aarch64 range walk is an internal implementation detail of the existing memory page-table TCB. It introduces no capability, domain assignment, trusted subsystem, external pointer, or cross-domain path and does not change which root or virtual range the caller is authorized to mutate."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The x86_64 permission rewriter caches a leaf-table pointer only after verifying each present non-huge PML4/PDPT/PD descriptor under the live root's existing mutation shard. Its cache key contains the complete upper-level index tuple and is invalidated at every 2 MiB boundary or after creating a missing table, preventing a descriptor write through a stale or unrelated leaf table. Each replacement retains the authoritative region's exact physical address and derived USER/WRITE/NX flags, and the complete changed span is locally invalidated before the guard is released and remotely invalidated before a peer-active rewrite returns. Direct descriptor replacement changes traversal cost, not frame ownership, mapping authority, or stale-translation retirement."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The cached x86_64 permission walk remains an internal optimization of the existing memory page-table TCB. It adds no capability, domain assignment, trusted subsystem, external authority, or cross-domain data path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The aarch64 scatter installer caches an L3 pointer only after ensuring or verifying every L0/L1/L2 table descriptor under the live root's existing mutation shard. The complete upper-level index tuple keys the cache and every 2 MiB boundary forces a new verified walk, so a leaf cannot be installed through an unrelated table. Fresh intermediate tables and leaves remain unpublished until the existing single DSB/ISB sequence; the permission-rewrite caller still completes the break and all-ASID inner-shareable invalidation before invoking this make phase. Lazy zero entries remain absent, existing leaves still fail as AlreadyMapped, and partial allocation or installation retains the prior partial-progress contract, so fewer walks change neither authority, backing ownership, nor descriptor publication ordering."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The cached aarch64 scatter installer is internal to the existing memory page-table TCB and reuses its root lock, frame allocator, and publication barrier. It adds no capability, domain assignment, trusted subsystem, external authority, or cross-domain path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The x86_64 scatter installer caches a PT pointer only after ensuring or verifying every PML4/PDPT/PD table descriptor under the live root's existing mutation shard. Its cache key contains the complete upper-index tuple and every 2 MiB boundary performs a new verified walk, so leaf stores cannot cross into an unrelated table. Each non-zero backing entry retains its caller-derived USER/WRITE/NX flags, lazy zero entries remain absent, existing leaves still fail as AlreadyMapped, and errors preserve the established partial-progress contract. The complete attempted span is locally invalidated before the root guard is released, including partial progress; large non-global runs use one local user flush while any run containing GLOBAL falls back to per-VA INVLPG. Batching therefore changes neither mapping authority, backing ownership, nor stale negative-translation retirement."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The cached x86_64 scatter installer remains internal to the existing memory page-table TCB and reuses its root lock, allocator, and local invalidation primitives. It adds no capability, domain assignment, trusted subsystem, external authority, or cross-domain path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Every successful x86_64 base-page or huge-page installation now ORs that leaf's independently derived USER/WRITABLE requirements into each verified non-huge intermediate descriptor after the destination leaf is proven absent; the permission-rewrite path performs the same promotion only after its complete structural walk succeeds. A supervisor leaf remains inaccessible because x86 permission checks AND the upper and leaf bits, so permissive table descriptors cannot override a leaf with USER or WRITABLE clear. Promotion preserves the descriptor's physical address and all existing flags, occurs under the live root's mutation shard, and is followed by the path's existing local invalidation. Failed AlreadyMapped or structural-error paths do not promote existing descriptors, preserving their no-permission-mutation behavior."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Intermediate x86_64 permission promotion is an internal correction to the existing page-table construction path. It adds no capability, domain assignment, trusted subsystem, external authority, mapped physical frame, or cross-domain path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Final-owner page-table teardown detaches only private top-level descriptors while holding the live root's existing mutation shard, after last-Arc ownership has proved that no CPU can execute or repopulate the root. The shared-alias transaction remains held through every leaf retirement, translation invalidation, and external backing release, and ends only after both authoritative region tables are drained; last-Arc ownership then excludes any new alias during private intermediate-table reclaim. Detached tables are traversed before bounded batch return; x86_64 unregisters and revalidates ownership at every level before making a table reusable, preserving the exclusion of copied kernel-shared tables."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Unlocked final-root traversal and batched table-frame return remain private implementation details of the existing memory domain manager TCB. They add no capability, domain assignment, trusted subsystem, user-visible mapping, external pointer, or cross-domain path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The unique-frame batch API is crate-private and unsafe: page-table teardown invokes it only after last-Arc ownership has made the root inactive, every data leaf has been retired, each private table has been detached, and x86_64 has removed the table's ownership-registry entry. Page-table frames never enter the data-page COW registry, so bypassing COW lookup cannot skip another owner; alternative allocators retain the ordinary batch implementation by default."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "x86_64 user tasks now use the already-established own-stack CPL3 timer-preemption path while retaining run-to-completion CPL0 syscall continuations. Separate policy bits prevent enabling userspace fairness from also making arbitrary kernel continuations preemptible before NARF has a preempt-disable counter. The CPL3 path switches only after validating that the live trap frame lies within the current task's dedicated kernel stack, marks the return as a suspended continuation for QSBR, saves the task FPU before clearing current-task publication, re-arms exactly that slot's waker, and restores the same continuation, address space, TLS base, and FPU state before iretq. It neither aliases task stacks nor permits one task to resume through another task's address space or register state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "User timer preemption remains inside the existing frame and scheduler executor TCB and uses its existing per-task stack, trap-frame, address-space, and run-queue ownership. It adds no trusted subsystem, capability, domain assignment, ambient authority, or cross-domain data path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Procfs basic task snapshots now obtain only saturating allocation-free aggregate counters while holding the existing address-space region locks. The scan never transfers physical ownership or exposes frame addresses, counts a base page resident only when its authoritative physical slot is nonzero, and converts huge leaves to base-page equivalents without changing mappings. Detailed per-VMA NUMA data remains opt-in for maps/numa_maps; ordinary liveness and metadata reads cannot allocate kernel memory proportional to an untrusted task's region count."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler refactor remains inside the already enumerated executor TCB. A separately packaged policy receives only copied scheduling metadata and opaque task handles; TaskSlot, futures, stacks, trap frames, domain state, run-queue mutation, budget charging, and switch routines remain executor-private. Linking a policy crate is not a security sandbox, and the specification explicitly retains trusted-for-availability review until protected-domain policy execution exists."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Policy installation still requires a live Cap<SchedPolicy, Grant>. Every returned handle is revalidated by the core against current queue identity, runnable state, affinity, and core-computed budget eligibility before removal or execution; invalid, stale, throttled, and wrong-tier choices fall back to a core-owned candidate. WorkKind and scheduling class are descriptive and grant no capability, domain right, CPU-hotplug authority, clockevent access, or DVFS control."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L62"
argument = "The policy, task-queue, and CPU-state hooks expose immutable scheduling metadata only: task identifiers, work/class/priority, runnable and affinity state, copied budget/accounting values, transition reasons, and aggregate idle counts. They expose no task memory, capability object, saved protection-key state, address-space root, stack, trap frame, future body, or mutable queue reference. Task enter/leave callbacks are serialized under CPU-local policy/run-queue locks and never execute from a waker or hard-IRQ path; CPU-state callbacks run after relevant core locks are released."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Core-owned period replenishment, eligibility tiers, bounded idle borrowing, and debt repayment cannot be overridden by policy. Borrowable work runs only when no eligible candidate exists, strict-throttled work stays ineligible until replenishment, and the tick forces a switch at the hard boundary. Domain state remains mechanism-owned below policy and is preserved by the architecture-private switch boundary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame boot edit only repeats the existing idempotent SysFs block-class projection after PCI probing has registered storage devices. It adds no TCB member, authority, domain transition, capability operation, mapping, or scheduler state; it exposes metadata already present in the block registry through the existing untrusted filesystem bridge."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The late SysFs projection reads block names, capacities, and immutable partition-identification metadata from the existing registry and creates only kernel-owned virtual kobjects. It does not dereference user memory, grant capabilities, access another domain's data, or modify block I/O, mount selection, or frame ownership."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Realtime service is now granted only by the scheduler core after a live Cap<CpuBudget, Spend> check and an atomic, ceiling-rounded reservation against per-CPU, online-system, and capability-object-domain limits. Generic spawn paths demote an unauthorised Realtime label; admitted tasks are own-stack, preemptible, CPU-pinned, and retain an RAII reservation that releases on every slot-drop path. Policy cannot alter the reservation, replenish runtime, move the task, or make throttled work eligible. Five percent of each CPU remains outside RT reservation for interrupt and kernel progress."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "AArch64 stackful tasks save AAPCS64 callee state, SP, resume PC, DAIF, and architecture-private domain state in task-owned context, validate every incoming stack/PC, and switch from the generic-timer tail only after GIC EOI and hard-IRQ accounting have ended. The live vector frame is required to lie inside the current task stack; current-task publication is cleared before switch-out and restored on resume, and involuntary returns remain non-quiescent for QSBR. Common vector entry neutralises domain state before Rust and the switch primitive restores it before the resumed continuation."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Scheduler hot paths use CPU-local run queues and policy/donation/steal publication, task-sharded affinity, pending-address-space, donor-debit, and NUMA registries, plus lock-free admission and interrupt counters; no single scheduler-wide lock serialises dispatch. Logical hot-unplug publishes Draining before new dispatch, refuses a running or pinned task, drains without holding source and destination queues together, and balances policy task-lifecycle callbacks. The power path receives only a copied per-CPU demand snapshot and gains no scheduler mutation, frequency-register, capability, domain-transition, or task-memory authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "AArch64 EL0 preemption stays within the existing arch, frame, userspace-kernel, and scheduler executor boundaries. It adds no trusted subsystem, capability kind, domain identifier, scheduling-policy authority, or externally callable switch primitive; stacks, trap frames, saved registers, and context switching remain hidden from policy crates."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The generic-timer path reaches the private scheduler switch only after common vector entry has saved the interrupted SCTLR/GCR state and entered neutral Frame state, and after GIC EOI plus hard-IRQ accounting complete. The live vector frame must reside on the published task's dedicated kernel stack. kernel_switch preserves the architecture-private domain snapshot, and the unchanged vector epilogue restores the interrupted rights immediately before ERET; no policy callback receives any of this state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Every AArch64 user switch-out saves Q0-Q31, FPCR, FPSR, and the live TPIDR_EL0 into task-owned stable storage before current-task publication is cleared. Dispatch activates the slot's retained address space and restores its TLS and FP/SIMD image before resuming the saved continuation; zero TLS has explicit validity and cannot leak a prior task's value. Fork/clone seed the child from live parent TLS and FP/SIMD state, while exec clears both as Linux arm64 does. Involuntary returns remain non-quiescent for QSBR, and syscall continuations remain non-preemptible except at explicit safe exit/park boundaries."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler edit adds only clippy-required safety documentation to existing AArch64 verification code. It changes no executable statement, context-switch precondition, saved state, scheduling decision, capability check, domain transition, or TCB boundary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The own-stack reset is compiled only under the kernel-test feature and is invoked only by the existing userspace test-hook cleanup. It restores the verification initial state between distributed smokes; production builds retain the one-way own-stack enablement contract, and no policy crate gains access to stacks, trap frames, saved domain state, or context-switch mechanics."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame boot edit removes all distro-specific writable tmpfs and home-overlay creation from the pre-PID1 path. The remaining bootstrap operation only makes the existing device, proc, and sys filesystems reachable inside the selected root; no new TCB member, authority, domain transition, capability operation, scheduler state, or physical-frame ownership path is added."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Writable runtime and home mounts are now requested by PID 1 through the existing Linux mount syscall boundary instead of being created with bootstrap mount authority. Removing the kernel policy does not widen user-memory access or bypass filesystem capability checks; typed mount construction and user-copy validation remain unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler change adds one opaque pointer field to the existing private KernelTask context and two mechanism-only accessors. It adds no trusted subsystem, policy callback, scheduling choice, capability, domain transition, address-space authority, or externally dereferenceable task state; scheduler code stores and returns the scalar pointer but never interprets its pointee."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Userspace publishes Arc::as_ptr(Task) only while the enclosing pinned UserTaskFuture retains that Arc through the full KernelTask lifetime. CURRENT_STACKFUL_TASK selects the pointer from the actually executing continuation across direct resume and migration, is cleared before switch-out/exit, and KernelTask reclamation remains RCU-deferred; acquire/release publication prevents a syscall from observing an uninitialized context, while cross-task lookups retain the refcounted registry path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Address-space materialization retains the authoritative region lock and exact backing vector while the existing architecture scatter installer holds the live root's mutation shard and derives each leaf's user, write/COW, and execute permissions from the same region metadata as the prior scalar path. Lazy zero backing remains absent. Because the scatter helper intentionally permits partial progress, every error is recovered through the idempotent scalar installer over the complete requested slice: an already-installed prefix and pre-existing leaves each acquire the same deduplicated reverse mapping, while a genuine structural or allocation error still fails construction before metadata or backing ownership is released. Successful batches add reverse mappings only after descriptor publication completes. Batching therefore changes lock and table-walk frequency, not mapped physical addresses, permissions, COW ownership, failure visibility, or stale-translation retirement."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Calling the already-specified x86_64 and AArch64 scatter installers from address-space construction is an internal optimization inside the existing memory domain-manager TCB. It adds no capability, domain identifier, trusted subsystem, externally supplied pointer, mapping authority, or cross-domain data path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "x86_64 signal delivery now performs every user-stack write through the architecture's existing guarded user-copy primitive. The earlier writability probe remains only an optimization: a sibling sharing the address space may revoke or unmap the page afterward, but the final copy converts that race into ordinary delivery failure instead of allowing a supervisor page fault or kernel panic. No kernel-half address passes validate_user_range, no signal-frame pointer is dereferenced directly, and no capability, domain right, or mapping authority is added."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Stack growth retains the authoritative region lock while locating and replacing only the explicit STACK_GUARD region. The optimized ordered lookup is preserved, but the adjacent unmarked VMA is no longer merged or assigned fresh anonymous backing: this prevents an unmapped-and-replaced stack neighbor from importing SHARED, FILE_DEMAND, or COW ownership semantics. Fresh frames remain private, zeroed, and mapped only at the promoted guard span; executable permission is copied only as a leaf authority hint without modifying the adjacent region."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The targeted-wake scheduler fast path is removed because it could bypass the active policy and compare priority without scheduling class, selecting Batch work ahead of Realtime work. Dispatch again uses the established core/policy selection and eligibility validation path exclusively; removing the unused hooks grants no new policy observation, task handle, capability, domain state, or queue mutation authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Demand-fault publication now keeps the backing slot lazy until leaf installation succeeds. On failure the exact page ticket is retired while ownership stays with the fault path, which releases the unpublished anonymous or file-backed frame; a later fault may safely claim the page again. On success the leaf, Region backing slot, reverse-map owner, and ticket retirement are completed under the authoritative region lock before overlapping VMA teardown can free the frame. This closes the x86 post-unlock stale-rmap race, gives AArch64 the same reverse ownership, and preserves ticket-based protection against VMA removal/replacement ABA."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Every x86 shootdown invalidation now reaches an architecture helper with compiler_fence(SeqCst) immediately before and after the TLB instruction. Plain INVLPG requests route through the fenced memory primitive, INVPCID variants share one fenced raw wrapper, and CR3 fallback already uses fenced control-register helpers. The fences constrain compiler motion only; request ownership, target completion, invalidation scope, mapping authority, and hardware instruction semantics are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Production order-0 frame allocation and return no longer update global cache hit, miss, refill, or spill counters. The counters and their round-trip assertion remain compiled under the kernel-test feature, while allocator decisions continue to depend exclusively on the unchanged per-CPU cache contents, drain/hotplug bypass state, free-page accounting, and buddy locks. Removing observational atomic RMWs changes no frame identity, ownership transition, NUMA choice, reserve check, cache capacity, or reclamation visibility."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Demand-fault ownership remains address-space and page scoped under the existing authoritative region lock, but the common live claims now occupy a fixed inline table and use a checked per-address-space ticket counter instead of allocating a BTree node and incrementing a global atomic on every first touch. Exact tickets still deduplicate same-page faults and reject owners cancelled by VMA removal/replacement. A rare overflow map preserves progress beyond the inline capacity, so CPU migration, hotplug, nested faults, and future concurrency cannot create an ownerless retry. Ticket exhaustion fails closed without identifier reuse, and no physical-address sentinel or mapping permission changes."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Anonymous demand allocation reports reserve pressure only after cancelling its exact page-scoped claim and returning through every address-space and allocator lock. Frame then publishes only the current task's scheduler-owned waker into a fixed MAX_USER_TASKS-sized table; it stores no task pointer, address-space pointer, frame owner, capability, or domain state. A sequentially consistent generation handshake orders registration against kswapd completion, slots remain owner-held through resume to prevent ABA reuse, and task-owned wake routing remains valid across migration and CPU hotplug."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The page-fault backpressure path allocates no memory and performs only a bounded fixed-table scan before switching out. It clears the per-CPU mempolicy publication before parking, invokes no wake callback while holding a waiter lock, and retries demand allocation at most once. Missing stackful context, full waiter capacity, non-pressure errors, and a second zero-progress failure all fail closed without sleeping or spinning, so trap progress cannot depend on an unbounded reclaim loop."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "TLB-shootdown verification no longer clears live SMP residency, idle, deferred-flush, or topology publication while scheduler CPUs may be active. Production residency filtering and the idle-to-busy debt handshake retain their sequentially consistent publication order; state-parameterized helpers expose that exact mechanism to isolated test atomics without granting new mapping authority or changing invalidation scope. The remaining live integration checks use monotonic counter deltas and a reserved tracked bucket, so they cannot erase another CPU's obligation or create a missed invalidation window."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler waker no longer performs a diagnostic fetch_add on one global cache line for every wake. The task-owned awake Release store, owner-CPU Acquire load, remote reschedule decision, Arc lifetime rules, queue ownership, policy selection, and migration behavior are unchanged. Removing the framebuffer's observational read grants no scheduling, capability, domain, or task-memory authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Production TLB shootdowns no longer update global diagnostic counters or recompute an all-peer accounting budget. Kernel-test and host-test builds retain the exact counters used by liveness and filtering smokes; production accessors return zero. Local invalidation, the mandatory StoreLoad fence, residency and idle-debt publication, target selection, IPI fanout, completion ordering, deferred full flushes, ASID/PCID scope, and backing-frame release order are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L89"
argument = "Slab hit/miss telemetry moves from one shared atomic per size class into the already cache-line-isolated per-CPU magazines and is aggregated only by read-only statistics calls. Allocation and free still select the same current-CPU magazine with interrupts masked, mutate the same stack/top state, preserve the global in-use ownership count, and take the same central-list and buddy locks on misses. Counter placement therefore changes no object identity, allocation authority, domain assignment, zeroisation, canary check, reclaim eligibility, or cross-domain memory access."
