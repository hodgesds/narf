[safety_argument]
schema_version = 1
agent = "OpenAI Codex GPT-5"
prompt_hash = "376409e8b766be629e598557ea11b309d2ba7ddd3f02f2dad99e620f0f5d5ec2"

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The only frame/ change forwards the existing kernel-test feature to narf-boot so validation smokes enter the distributed test slice. It adds no production code, authority, memory access, domain transition, or trusted subsystem; release builds leave the feature disabled."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame manifest only forwards the existing kernel-test build feature to the untrusted Btrfs driver so its distributed tests and embedded fixtures are linked exclusively into verification kernels. Production frame code, authority, domain transitions, mappings, capabilities, scheduling state, and the TCB boundary are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit only forwards the interrupted instruction pointer already present in its trap frame into the existing IRQ dispatcher; it adds no new TCB member or authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "NUMA procfs reporting copies only virtual metadata and aggregate node counts while the address-space region locks are held; it never exports physical addresses, clones HugeFrame ownership, or changes mapping/domain rights."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Huge mappings validate virtual/physical alignment before installing architecture leaves, retain authoritative frame ownership in AddressSpace, invalidate translations before returning frames to the pool, and preserve W^X plus NUMA/cpuset selection without changing domain assignment."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Explicit NUMA syscalls and cpuset.memory_migrate replace authoritative base-page or complete huge-leaf backing and flush peer TLBs before old frames return to the buddy or huge pool. Failed huge-leaf replacement restores the original mapping and retains its ownership. Shared mappings are never moved without MOVE_ALL authority, which NARF rejects because it has no ambient CAP_SYS_NICE."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Correcting Linux mempolicy flag values and adding PREFERRED_MANY only changes the order in which frames are requested from existing per-node allocators. The cpuset allowed mask remains a hard boundary, and frame ownership, domain assignment, and mapping authority are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Weighted interleave changes only target-node selection before existing owned frame and huge-frame allocation. The policy mask remains intersected with cpuset.mems, weights cannot be zero, and no mapping, ownership, domain-rights, or authority checks are bypassed."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "HMAT automatic weighting publishes bounded allocation ratios through atomics under a configuration lock. Firmware coordinates cannot expand a task's cpuset policy mask or grant mapping/domain authority, and missing coordinates fail rather than fabricating topology."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Moving interleave sequence ownership from CPU slots to task-scoped userspace-compat state changes only deterministic target selection. The active fault policy still intersects cpuset.mems before entering the allocator, and task teardown reclaims the sequence state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Automatic NUMA balancing samples only private, unlocked user leaves and retains ownership of every backing frame while the leaf is absent. Hint resolution intersects policy with cpuset.mems, serializes migration through the address-space region lock, completes cross-CPU TLB invalidation before freeing the source, and always restores a mapping after migration failure."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Memory hotplug admits only aligned, non-overlapping host-acknowledged RAM already covered by the active kernel linear map. Online donation cannot grow allocator metadata under the frame lock; offline is an exact-range, all-free transaction whose failed validation leaves buddy lists and per-node accounting unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "virtio-mem preserves physical-frame ownership by ordering PLUG before allocator donation and allocator removal before UNPLUG, rolling either side back when the second step fails. No busy frame is reclaimed or exposed to the host, and dynamic node reporting exports aggregate state rather than physical addresses."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "HMAT memory tiers only constrain target selection: demotion requires a strictly slower online tier and intersects candidates with the caller-provided cpuset/mempolicy mask. Unknown firmware coordinates remain in the default top tier rather than silently weakening placement policy."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Tier demotion delegates to the existing private-page and complete-huge-leaf migration transaction, preserving ownership and permissions, rolling back failed leaf replacement, and completing cross-CPU TLB invalidation before returning old backing to the allocator."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Shared-memory migration serializes alias creation with replacement, retains both frames through all address-space updates, rolls back every changed alias before failure returns, commits registry ownership only after all PTEs change, broadcasts TLB invalidations, and frees the source only after commit. Device/DMA mappings are rejected."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Memory-block membership is derived from authoritative boot and hotplug physical RAM ranges classified by SRAT proximity, never inferred from CPU placement or fabricated for absent memory."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Externally owned shared pages carry one registry reference per address-space alias. Handle removal prevents new aliases but defers frame reuse until every mapped alias has torn down its leaf and completed cross-CPU TLB invalidation; mapping, unmapping, MAP_FIXED replacement, fork aliasing, and migration use one shared transaction order."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The memory-hotplug observer runs only after allocator donation/removal and topology-registry commit, with both locks released. It receives no physical-frame ownership and can only rebuild read-only sysfs topology; failed and busy transactions emit no notification. Offline ranges remain recorded solely as non-allocatable topology."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "PMI dispatch runs after the trap prologue has entered the Frame domain and only copies scalar task/IP/time values into kernel-owned per-CPU storage; it does not bypass capability checks or alter domain tags."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler PMU hook receives only the already-published scalar TaskId and a run-state edge. It runs outside run-queue locks, grants no capability, and cannot select or resume a task."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "PMU switch-out disables and releases the current CPU's counter before executor code or a peer task runs. Per-CPU allocation prevents one logical CPU from programming or releasing another CPU's MSR bank during migration."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The aarch64 boot edit passes only a firmware-validated private interrupt number from the bus parser to the GIC. The GIC rejects non-PPI INTIDs, applies the route only to each CPU's own redistributor, and grants no capability or cross-domain memory access."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The aarch64 IRQ edit publishes only the interrupted ELR scalar through the existing per-CPU interrupt context slot after the trap prologue has entered the Frame domain. It neither dereferences task memory nor changes domain rights, capabilities, or scheduling state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The NUMA topology fallback is compiled only for hosted crate tests, where the narf-frame ACPI bridge is absent. Kernel targets still require the authoritative frame-provided hooks, so production frame placement, ownership, cpuset constraints, and domain assignment are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Adding NetfilterAdmin as a distinct stable capability kind keeps firewall authority in the existing capability-table TCB and does not expand the trusted subsystem set."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "A netfilter control operation requires a live epoch-checked capability, sufficient explicit operation rights, and an exact immutable network-namespace-id match. No uid, Linux ambient capability bit, namespace membership, or raw userspace slot bypasses those checks."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L70"
argument = "NetfilterAdmin copies share one capability-table object epoch; revoking one invalidates every copied handle at its next authority check while revealing only the already documented stale-cap revocation bit."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The procfs adapters read only the scheduler-published current task id, resolve its immutable network-namespace identity through userspace compatibility state, and request read-only aggregate snapshots. They neither dereference userspace memory nor modify capability, domain, frame, or scheduling state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Network namespace lifetime is retained by typed Arc ownership in tasks, namespace fds, and sockets. Final-reference teardown removes only state keyed by that immutable namespace id, returns physical interfaces to namespace zero after releasing registry locks, and does not alter memory domains, frame ownership, capability epochs, or scheduler state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Perf code-page-size sampling reads only registered virtual-region bounds and the hardware leaf-size enum under existing address-space locks. It exports no physical address or frame handle, transfers no ownership, and does not modify page tables, domain rights, capabilities, or scheduler state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Perf privilege filtering changes only the current CPU's exclusively allocated PMU selector bits. Existing ownership checks still guard allocation, rearm, pause, read, and release; the filters grant no memory access, capability, domain right, or cross-CPU register authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Perf IRQ capture copies scalar registers from the already-saved architecture trap frame after the trap prologue has entered the Frame domain. The dispatcher publishes them only in kernel-owned per-CPU atomics for the synchronous handler walk; no trap-frame pointer, capability, domain right, or scheduling authority escapes."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "User-stack sampling resolves bytes only through the sampled AddressSpace's locked, authoritative resident backing metadata and the kernel direct map. It stops at unmapped or lazy backing, never faults user pages in IRQ context, never exposes physical addresses, and retains all frame ownership."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit changes only the diagnostic time budget attached to the existing verification-only Btrfs interop initcall. It adds no trusted subsystem, execution path, authority, capability, mapping, domain transition, or ownership change; the initcall body and result handling are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler and frame edits add forward-progress accounting, idle-state reporting, and fatal-path stall diagnostics inside the existing TCB. They add no trusted subsystem and do not move the documented TCB boundary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Forward-progress counters and queue diagnostics neither bypass nor alter capability checks, domain tags, PKRS state, or domain transitions. The watchdog reads aggregate scheduling state and can only diagnose or panic after runnable work has made no progress."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L62"
argument = "The new scheduler observations are aggregate queue depths, a monotonic progress counter, and fatal-path task state. They remain within the existing documented per-CPU scheduling-state exception and do not expose task memory or capabilities to userspace."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The direct PID 1 loader remains inside the existing frame TCB and only publishes a task after the existing ELF loader has built its address space and the task's explicit filesystem-root metadata is installed. It grants no capability, changes no domain assignment, and removes a userspace chroot launcher rather than adding an authority-bearing intermediary."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The rooted interpreter lookup reads bytes only through the existing VFS resolver under the explicit mounted root and retains the normal ELF-loader validation path. It does not dereference untrusted user pointers, alter page mappings outside the loader, or bypass capability or domain checks."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The cgroup memory-charge resolver maps an executor-private TaskId to the already authoritative userspace ProcessId before consulting existing cgroup membership. It adds no capability, domain transition, memory mapping, or frame-ownership path; unmapped kernel tasks remain deliberately unattributed rather than being charged to an accidental numeric TaskId match."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit only links the untrusted SquashFS driver crate and invokes its initcall-registration function alongside existing filesystem drivers. Parsing and block I/O remain in DomainId::DRIVER_0 behind the existing filesystem/block interfaces; no capability, domain transition, frame ownership, scheduler state, or TCB boundary changes."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit embeds a verification-only static test binary in the existing boot-init fixture. It adds no production execution path, authority, mapping, capability, domain transition, or ownership change."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The mount-change bridge invokes the existing global readiness notifier only after filesystem state has committed and released its mount-table lock. It carries no path, task, capability, mapping, domain, or ownership authority; waiters independently re-query their own namespace-scoped procfs view."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Two-CPU user-task placement changes only the preferred initial run queue within the existing CpuSet::ALL allowed mask. It does not expand the TCB, alter a task's domain, capability, address space, or saved security state, and larger topologies retain the existing AP-only policy."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The placement helper selects only already-online scheduler CPUs and enqueue_on continues to enforce the existing task metadata and domain restore path before polling. Alternating BSP/AP grants no memory or capability authority and never schedules outside Affinity.allowed."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Runtime affinity changes remain inside the existing scheduler TCB and change only a live task's allowed online CPU set. The task retains its TaskId, address space, domain, capabilities, saved protection-key state, and dedicated kernel stack across the cooperative requeue."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "A parked task is removed under its sole run-queue lock and enqueued only after that lock is released; a currently-executing task is never moved mid-continuation and consumes the registered mask only after returning to the executor. Every dispatch and work-steal path checks the hard allowed mask before restoring task state."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Task creation validates caller-provided affinity against the online topology before publishing either the run-queue slot or registry entry. Because the legacy spawn API is infallible, a wholly offline mask is normalized to the spawning CPU while a partly online mask is retained for hot-plug; this prevents an awake slot from cycling forever without weakening any valid hard affinity constraint."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Post-poll requeue keeps the CPU that just executed the continuation whenever it remains online and allowed. Affinity.preferred is only an initial-placement hint; it cannot force an otherwise-unnecessary cross-core context migration or bypass the hard allowed mask."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The BPF memory work stays inside the existing frame/ + memory/ TCB and adds no new trusted subsystem. `bpf_text::reserve_kernel_slots` runs as a direct call from bare_main after the MMU handoff and before the first `new_user_pml4`, because `new_user_pml4_on` snapshot-copies PML4[256..511] by value with no propagation; a slot first populated after a user address space exists would leave that CR3 holding a zero entry and triple-fault. A `debug_assert` in `new_user_pml4_on` makes a future reordering fail loudly, and an in-kernel smoke reads the live root to assert both entries are present and both arena guard slots are absent."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Every BPF kernel-VA mapping is installed with USER clear at the leaf and at every intermediate level, so no CPL=3 / EL0 walk can reach BPF text, arena pages, or the per-CPU BPF stack. Arena frames are zeroed on population so a recycled frame cannot leak its previous owner's bytes to a program. The extable consults a per-image table keyed on the faulting PC and recovers only kernel-mode faults; it neither bypasses nor alters any capability check or domain tag."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "`CapKind::Jit` gates both executable-text allocation and the only mprotect path that can produce a W|X user mapping. Both entry points re-check liveness on every call rather than trusting possession, per invariant #5; the grant table is swept by the thread-scoped exit observer so an authority cannot outlive its task; and adding W to an already-executable mapping is refused regardless of the capability. `AddressSpace::mprotect_range` continues to reject W|X outright, so exactly one capability-gated function can construct such a mapping."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Kernel mappings are NX by default and the remaining W^X gap is recorded rather than claimed closed. `memory/src/x86_64/mmu.rs` now builds the low identity map, the high-MMIO window, the kernel direct map and the higher-half kernel window with NO_EXEC, demoting the identity map 1 GiB -> 2 MiB -> 4 KiB and the kernel window 1 GiB -> 2 MiB so the only executable ranges are [__kernel_start, __text_end) and the 8 KiB AP-trampoline window at physical 0x8000; `frame/src/aarch64/boot.S` does the same with PXN|UXN over both 1 GiB RAM blocks. Both demotions are constructed before the CR3 / sctlr_el1 handoff, so no live mapping is ever split and no break-before-make window exists. The AP trampoline sets EFER.NXE before CR0.PG, because with NXE clear PTE bit 63 is a reserved bit and the first post-paging fetch would triple-fault. The x86_64 S3 wake trampoline in `arch/src/x86_64/s3_resume.rs` has the same dependency and now discharges it the same way: firmware is permitted to clear IA32_EFER across S3, so `save_resume_context` snapshots EFER (with LMA masked off, as Linux does for `tr_efer`) and `s3_wake_entry` replays it with `wrmsr` BEFORE the `mov cr3` that switches to the NX kernel tables — otherwise the first access through any kernel mapping, data or fetch, would take a reserved-bit #PF with no IDT loaded. This is reasoned-and-unit-tested, not boot-proven: S3 remains gated off behind `REAL_SLEEP_ARMED` / `PRODUCTION_S3_ENABLED` and QEMU/TCG gives no real suspend/resume cycle, so the smokes in `arch/src/tests.rs` (`arch/s3`) pin the saved EFER's shape (LMA masked, LME and NXE set) and the `offset_of!`-derived displacements the asm indexes, rather than observing a resume. Consequently no kernel data has an executable alias, and BPF JIT text is executable at exactly one address. It is no longer WRITABLE through that alias either: `memory/src/text_poke.rs` `protect_ro`, called from `bpf_text::seal`, clears WRITABLE / sets AP_RO_EL1 on every kernel window aliasing the pack's frames — the low identity map, the higher-half kernel window, and the direct map where one exists — so an arbitrary kernel write can no longer overwrite live JIT text. On x86_64 a live huge leaf in the way is split following Linux `__split_large_page` (`arch/x86/mm/pat/set_memory.c:1121`): the replacement table translates identically to the leaf it replaces, so Intel's TLB application note (undefined only when the large and small translations DIFFER) does not apply, the swap is one naturally-aligned 8-byte store, and the attribute change happens only after a synchronous global flush (`flush_user_tlb_all_cpus`, which spins for every peer's ack), which is Linux's split -> flush_tlb_all -> change ordering; `bpf_text`'s PACKS lock serialises it as `cpa_lock` does. This also required setting CR0.WP, which NARF's boot path never did (measured CR0 was 0x80000011) and without which a supervisor store ignores the R/W bit entirely (Intel SDM Vol 3 §4.6.1) — `text_poke::enable_write_protect` sets it on the BSP and on every AP, since WP is per-CPU state. Writes into an already-sealed pack go through `text_poke::poke_copy`, a per-CPU RW+NX alias of one 4 KiB frame torn down before the call returns, and `reclaim` calls `protect_rw` before frames go back to the buddy. One case is deliberately NOT closed: an aarch64 fallback pack (scattered 4 KiB frames, built only when the hugepage pool is empty) would need a live 2 MiB block split, which ARMv8 requires break-before-make for and which would unmap live kernel memory on every other CPU — Linux arm64 declines the same split for the same reason. `can_protect` refuses it, `seal` leaves the pack's alias writable, and `bpf/specification/spec.md` §4.2 / §8.6 record the trade. In-kernel smokes in `memory/src/tests.rs` prove the alias faults on instruction fetch through both windows and that the trampoline window is exactly two executable pages wide; `memory/src/bpf_text.rs` `smoke_bpf_text_sealed_alias_write_faults` performs the write that worked before this change through the linear alias and requires a #PF with the present+write error bits set and the byte unchanged, and `smoke_bpf_text_reclaim_restores_writable_alias` proves the frames go back writable."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "`validate_user_range` — the single choke point every `copy_to_user` / `copy_from_user` / `copy_from_user_vec` / `copy_user_cstr` call site in `userspace/` funnels through — now confines BOTH the first and the last byte of a syscall's range to the user half (`addr < 1 << 47`, Linux's `__access_ok` against `TASK_SIZE_MAX`). It previously accepted canonical kernel-half addresses on the recorded theory that SMAP enforced the boundary. That argument was inverted: SMAP faults a CPL-0 access to a page with PTE.U=1, says nothing about a kernel page (U=0), and `copy_user_guarded` runs its `rep movsb` inside a STAC/CLAC bracket that disables SMAP outright — so a mapped kernel destination was written silently, the guarded copy's fault path never fired, and the caller got Ok(()). Any syscall with a caller-supplied (address, length, content) triple was therefore an arbitrary-kernel-write primitive; `bpf(BPF_BTF_LOAD)` followed by `bpf(BPF_OBJ_GET_INFO_BY_FD)` with `bpf_btf_info.btf` in the kernel half was the demonstrated gadget, and the read direction was the matching info leak. The check is unconditional in every production build: the only relaxation is `handlers::kernel_buffers_guard()`, which is `#[cfg(feature = \"kernel-test\")]`, dynamically scoped to a block, keyed on the calling CPU, and still refuses a range that straddles the canonical hole. It is held by the `syscall_abi` fixture harness and 125 named in-kernel smokes (126 guard sites, one of which is `with_kernel_buffers` itself) that hand kernel `.rodata`/stack/heap pointers to the syscall entry point; every other test in the `kernel-test` build, including the seven in `userspace/src/abi_uaccess_tests.rs` and `smoke_uaccess_validate_canonical_holes`, exercises the production predicate."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Removing BuddyZone::free's vestigial overlap scan changes only the complexity of the existing allocator free path. Frame accounting and standard buddy coalescing remain unchanged inside the memory TCB, and no new trusted subsystem or ownership path is introduced."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The x86_64 trap-entry edit remains inside the existing frame TCB and only normalizes the live direction flag before compiler-generated kernel code executes. It adds no trusted subsystem, authority, domain transition, mapping, or ownership path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The CPU has already saved the interrupted RFLAGS in the trap frame before CLD executes, and IRETQ restores that saved value unchanged. Clearing only the live kernel DF prevents attacker-controlled CPL3 flag state from reversing kernel string operations without changing capability checks, domain tags, saved user state, or return authority."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The scheduler edit adds accounting callbacks at existing x86_64 stackful switch boundaries inside the already enumerated executor TCB. It does not add a trusted subsystem, change task selection, alter a task's saved domain state, or move any capability or address-space ownership."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Timer-preemption accounting reads only the current task's scalar timestamps while its continuation is pinned on the current CPU. The pause callback completes before CURRENT_STACKFUL_TASK is cleared, the resume callback runs only after the same continuation is current again, and both use IRQ-safe per-task accounting locks. Normal syscall entry creates the task's BTreeMap row before publishing the active span, so the timer-IRQ close path only updates an existing node and cannot allocate; no user pointer, capability, domain right, or cross-task memory is exposed or modified."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The removed scan neither enforced capability checks nor assigned domain tags; it leaked an overlapping free after an ownership violation had already occurred. The audited address-space and page-table ownership fixes remain in place, while KASAN-capable kernel tests and stress teardown continue to exercise double-free and use-after-free failures."


[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The frame edit only force-links the optional narf-bpf-leds seam and registers its initcalls beside the existing BPF consumer anchor. The new kfunc, bounded mailbox, LED registries, and worker remain outside the documented TCB; no trusted subsystem, capability authority, domain assignment, task state, or memory-mapping path is added."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "narf_led_submit accepts scalars only and, in atomic BPF context, performs a bounded sequence of atomic operations into fixed FRAME-visible storage. Device lookup, allocation, trigger locking, and potentially sleepable hardware access occur later in one Stage::Late worker. Unknown actions fail with EINVAL, queue saturation fails with EAGAIN, full-width invalid indices remain drain-time no-ops, and the kfunc neither dereferences program pointers nor changes capability or domain rights."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Dynamic packet bounds change only the existing untrusted BPF verifier/runtime and Linux-compat attach path; no frame, memory-domain-manager, capability-table, scheduler-executor, or security-model code is modified, and no subsystem is added to the documented TCB."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "An XDP packet address is constructed only from the classifier's live immutable frame borrow after a BpfAttach-authorized type-correct attach. The verifier requires a same-provenance unsigned data/data_end comparison covering the complete access; raw-context execution is refused; the interpreter independently checks the exact slice; and native code is admitted only at the verifier-certified site. Execution remains inside the existing FRAME+BPF PKS mask, so the feature exposes RX bytes reachable there and grants no access to driver-private state or any additional domain."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "XDP test-run changes only the existing BPF runtime and Linux-compat syscall adapter. It modifies no frame, memory-domain-manager, capability-table, scheduler-executor, or security-model code and adds no trusted subsystem."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The syscall range-validates and copies data_in into a size-bounded kernel-owned allocation before execution, then BpfProg::run_xdp alone constructs data/data_end from that exact immutable slice. Caller-supplied ctx pointers, CPU selection, flags, and batch mode are rejected; raw XDP execution remains unavailable; repeat is bounded; output copies only the owned frame prefix through the guarded user-copy path; and verifier, interpreter-region, and JIT admission checks remain unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "Native arena-atomic lowering changes only the existing untrusted BPF JIT and runtime plus their specification and tests. It modifies no frame, memory-domain-manager, capability-table, scheduler-executor, or security-model code and adds no trusted subsystem."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The verifier remains the sole admission path for native arena atomics. Both emitters derive the address from the page-aligned kernel arena slot base plus the verifier-tracked zero-extended handle and signed instruction displacement, reject misalignment before any atomic access, and register the actual faulting instruction for recovery before executable publication. Natural-alignment and page-fault paths return distinct terminal statuses with the offending handle; fetching bitwise arena atomics preserve the portable fail-closed boundary and interpret instead of using x86 scratch state that would destroy that handle."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L39"
argument = "The production fix only moves the existing TSS.rsp0 and per-CPU SYSCALL-stack hook installation from the x2APIC-only IPI branch to the common x86_64 LAPIC initialization path. Frame and the scheduler executor remain within the existing TCB set; no subsystem, authority, or TCB boundary is added."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "The retarget hook continues to write only the current CPU's existing TSS.rsp0 and PerCpu.kernel_stack_top from a scheduler-owned task stack bound. Installing it for xAPIC restores the same per-task isolation already used by x2APIC and prevents user syscalls from sharing and corrupting the executor stack; capability checks, domain tags, address spaces, and task ownership are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Private base-page teardown skips the external shared-alias transaction only while holding the address-space region tables continuously from SHARED classification through removal. Shared unmap and range punching retain transaction -> huge-region -> regular-region ordering through leaf removal, cross-CPU TLB invalidation, and the backing owner's release hook. A racing map cannot change the classification without the held region lock. The x86_64 range helper holds the same per-root page-table mutation lock across the batch and caches only non-owning leaf-table pointers while that lock excludes replacement. Small batches retain per-leaf INVLPG; large batches clear every leaf before one compiler-fenced local non-global flush. Callers retain the existing cross-CPU flush-before-frame-release ordering. Private COW ownership is unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Range-scoped mmap materialization holds the authoritative address-space region lock continuously while selecting current backing metadata and installing leaf PTEs, so a CLONE_VM peer cannot unmap and free a cloned snapshot beneath the walk. It validates a non-empty aligned user-half interval and reuses the existing per-architecture permission derivation, W^X policy, map-error handling, and frame ownership; it changes only which current region page slots are visited and grants no new capability or domain access."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Fork COW state is represented independently from logical POSIX WRITE authority. Page-table derivation permits a writable user leaf only when WRITE remains granted and either no COW sharing exists or the authoritative frame refcount is one. The write-fault path requires both WRITE and COW before copying, so mprotect(PROT_READ) cannot restore access. aarch64 user mappings use EL0 AP encodings, and lazy zero sentinels never become leaves."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Private-region relocation holds huge and regular region locks from validation through publication, preinstalls a disjoint destination before removing source leaves, and retains source bookkeeping for rollback on page-table failure. A destination accepted by both region tables is PTE-free under the mapping invariant because every metadata removal first tears down its leaves; an unexpected leaf is rejected by installation and rollback rather than overwritten. Scatter installation holds the existing per-root page-table mutation lock across the batch, derives each page's unchanged COW/user permissions, and skips lazy zero sentinels. Source translations are invalidated on every CPU before a truncated tail is returned to the allocator; shared and huge ownership shapes are rejected rather than approximated."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "mlock validates complete mapped coverage before mutation, resolves file-demand pages through their external owner, allocates anonymous backing with no region lock held, revalidates before publishing, and frees every unconsumed or failed allocation. Exact VMA splitting moves rather than clones physical lists, preserving private, COW, and borrowed-frame ownership; LOCKED changes reclaim eligibility only and grants no mapping permission."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "mprotect and madvise preflight complete range coverage before changing permissions or releasing backing, so a hole cannot cause a partial security-state change. Permission rewrites preserve internal LOCKED/COW flags, derive hardware access from the resulting logical authority, skip lazy zero sentinels on both architectures, and keep W^X rejection in the existing capability-gated transition model."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Residency queries hold the huge-region and regular-region locks in the established order for one coherent snapshot, validate complete coverage before returning any result, and expose only one resident/nonresident bit per requested user page. They neither materialize lazy backing nor reveal physical addresses, frame ownership, COW reference counts, capability state, or domain tags; the userspace mincore adapter copies only this bounded bit vector through the guarded user-copy path."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The base-page region table remains ordered by virtual base after every operation that can move or append an entry. Since overlap admission still rejects intersecting intervals, predecessor/successor checks are equivalent to the former full scan. Empty MAP_FIXED punches return before touching page tables or backing; overlapping punches retain the existing under-lock leaf removal, cross-CPU invalidation-before-release, and authoritative Region.phys ownership path. Keyed removal does not change which region or frames are retired."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Exact-range LOCKED flag updates use the ordered, non-overlapping VMA invariant to drain only intersecting tree entries. Each affected Region.phys vector is still consumed exactly once into disjoint head/middle/tail fragments, unchanged regions remain in place, and reinsertion derives each key from its fragment base. This changes metadata movement only: eager population, leaf permissions, COW/shared ownership, and failure-before-mutation coverage validation are unchanged."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "Overlapping MAP_FIXED and munmap punches use the ordered, non-overlapping VMA invariant to classify shared mappings and drain only intersecting tree entries. Each removed Region.phys entry still follows the existing under-lock leaf teardown and shared-release or private-free path; prefix and suffix fragments remain disjoint and are reinserted by base. Unchanged regions retain ownership in place, and cross-CPU invalidation still precedes private frame reuse."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The base-page VMA representation changes from a sorted vector to a BTreeMap keyed by Region.base. Structural mutation is private to RegionTable: insert derives the key from the value, relocation and stack growth remove the old key before changing base, and range splits drain owned Region values before inserting disjoint fragments. Tree removal therefore transfers each Region.phys vector exactly once into the same leaf-teardown, COW/shared release, or surviving-fragment paths; it changes lookup and metadata movement complexity without changing frame ownership, page-table mutation, or invalidate-before-reuse ordering."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L42"
argument = "Ordered-tree point lookup returns only the greatest VMA base not above the requested user address and then checks the region end before exposing or mutating backing. Permission derivation, user-half validation, W^X checks, COW authorization, and domain/page-table flags are unchanged; the index neither grants a mapping nor bypasses any capability or domain check."

[[invariants_preserved]]
ref = "security-model/specification/spec.md#L75"
argument = "The page-table ownership registry remains fixed-size, allocation-free, and lock-free. Open-addressed insertion uses compare-exchange and never replaces a live physical-frame marker; lookup follows a collision chain until a never-used slot; unregister publishes a tombstone rather than an empty slot so later colliding registrations cannot become false negatives. A full registry still fails closed by retaining every existing ownership marker and incrementing the overflow counter. Consequently free_user_pml4_tree continues to reclaim only registered AS-private tables and cannot mistake copied kernel-shared page tables for owned frames."
