[package]
name         = "narf-bpf-leds"
description  = "BPF -> LED control: the narf_led_submit kfunc that enqueues brightness/blink/color commands for the LED engine worker to apply."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_bpf_leds"
path = "src/lib.rs"

[dependencies]
narf-bpf          = { path = "../bpf", version = "0.1.0" }
narf-drivers-leds = { path = "../drivers/leds", version = "0.1.0" }
narf-init         = { path = "../init", version = "0.1.0" }

# Test-only: the smokes assemble and load a program that calls the kfunc.
narf-lib          = { path = "../lib", version = "0.1.0", optional = true }
narf-bpf-isa      = { path = "../bpf/isa", version = "0.1.0", optional = true }
narf-bpf-verifier = { path = "../bpf/verifier", version = "0.1.0", optional = true }
narf-kernel-test  = { path = "../verification/kernel-test", version = "0.1.0", optional = true }

[features]
kernel-test = [
    "dep:narf-kernel-test",
    "dep:narf-bpf-isa",
    "dep:narf-bpf-verifier",
    "dep:narf-lib",
]

[lints]
workspace = true
