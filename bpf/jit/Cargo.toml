[package]
name = "narf-bpf-jit"
description = "BPF native code generation. Emits into a caller-provided buffer; host-testable against golden encodings."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

[features]
default = []
kernel-test = ["dep:narf-kernel-test"]

[dependencies]
narf-bpf-isa = { path = "../isa" }
narf-bpf-verifier = { path = "../verifier" }
narf-kernel-test = { path = "../../verification/kernel-test", optional = true }

[lints]
workspace = true
