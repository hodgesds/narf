[package]
name         = "narf-boot"
description  = "Bootloader handoff (multiboot2 / U-Boot FDT), BootInfo validation."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_boot"
path = "src/lib.rs"

[dependencies]
narf-lib    = { path = "../lib", version = "0.1.0" }
narf-memory = { path = "../memory", version = "0.1.0" }
narf-firmware-fdt = { path = "../firmware/fdt", version = "0.1.0" }
narf-kernel-test = { path = "../verification/kernel-test", version = "0.1.0" }

[features]
kernel-test = []

[lints]
workspace = true
