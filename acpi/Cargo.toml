[package]
name         = "narf-acpi"
description  = "Minimal ACPI table walker — RSDP / XSDT discovery + SRAT (NUMA topology) parsing."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_acpi"
path = "src/lib.rs"

[dependencies]
narf-lib         = { path = "../lib", version = "0.1.0" }
narf-memory      = { path = "../memory", version = "0.1.0" }
narf-arch        = { path = "../arch", version = "0.1.0" }
narf-kernel-test = { path = "../verification/kernel-test", version = "0.1.0" }

[lints]
workspace = true
