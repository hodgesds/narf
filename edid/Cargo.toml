[package]
name         = "narf-edid"
description  = "VESA E-EDID 1.4 parser. Clean-room from public VESA spec."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_edid"
path = "src/lib.rs"

[dependencies]
narf-kernel-test  = { path = "../verification/kernel-test" }

[features]
kernel-test = []

[lints]
workspace = true
