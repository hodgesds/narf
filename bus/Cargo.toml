[package]
name         = "narf-bus"
description  = "Boot-time device enumeration: PCIe ECAM (x86_64) + devicetree virtio-mmio (aarch64). Stage 2 → 3."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true

[lib]
name = "narf_bus"
path = "src/lib.rs"

[dependencies]
narf-lib          = { path = "../lib" }
narf-memory       = { path = "../memory" }
narf-boot         = { path = "../boot" }
narf-capabilities = { path = "../capabilities" }
narf-interrupts   = { path = "../interrupts" }

[lints]
workspace = true
